models:
  Car:
    brand: string
    slug: string unique
    year: timestamp
    description: mediumtext
    model_id: id
    user_id: id
  Model:
    name: string
    slug: string unique
    relationships:
      hasMany: Car

